import styled from "styled-components";

export const Item = styled.li`
  font-size: 1.125rem;
  margin: 0rem 0.5rem;
  cursor: pointer;
  svg {
    vertical-align: sub;
    padding: 0 0.5rem;
  }
  span {
    display: inline-block;
  }
`;
